using System.Collections;
using System.Collections.Generic;
using UnityEngine;

/// <summary>
/// A static class used to aid in the updating of tags. If a tag name is changed,
/// it only needs to be changed in this location, and not across multiple scripts.
/// </summary>
public static class Tags
{
    public const string ENVIRONMENT = "Environment";
}
